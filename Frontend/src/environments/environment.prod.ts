export const environment = {
  production: true,
  // Replace with your deployed backend API URL (no trailing slash)
  // Example: 'https://api.yourdomain.com/api'
  apiUrl: 'https://<YOUR_BACKEND_DOMAIN>/api',
};
